
  # Replicate Design

  This is a code bundle for Replicate Design. The original project is available at https://www.figma.com/design/p6yzmaeY5FQxP3HbbytENI/Replicate-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  